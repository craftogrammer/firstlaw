# Research audit trail

This folder contains per-subagent research logs, one file per run:

    <subagent-id>-<YYYY-MM-DD>.json

The orchestrator does not read these. They exist for later human or agent audit of how a contract was derived. Keep them, delete them on retention policy, or commit them — the kit has no opinion.
